#heroku.yml takes the place of the Procfile. No need to declare Procfile if heroku.yml present.
#Process (ps) containing front end container must be "web"
build:
    docker:
        api: api/Dockerfile
        web: web/Dockerfile
    config: #available to build environment
        PORT: 6000
run:
    api: npm start
    web: npm start

